import { Card, Typography } from "@codat/orchard-ui";
import * as React from "react";

const Home: React.FC = () => (
  <div id="page-home">
    <Card>
      <Typography variant="heading_xxl" as="h1">
        Homepage
      </Typography>
    </Card>
  </div>
);

export default Home;
